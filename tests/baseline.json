{
  "tests": 989,
  "coverage_bp": 9800,
  "why": [
    "The number of tests the suite must not drop below.",
    "",
    "A suite can shrink without any gate noticing. A file whose import breaks",
    "still reports its neighbours as green; a describe block deleted in a merge",
    "resolution leaves no trace; a refactor can lose cases silently. The other",
    "gates all answer 'did what ran pass' — none answers 'did everything still",
    "run'. This is a ratchet, not a target: it fails on a DECREASE only.",
    "",
    "Bumped to 487 with kernel-entities-law, to 574 with gate-coverage-strict-path, and again with module-deadlines-civil-date and module-deadlines-thresholds — the two halves of one split, which landed separately and each added 53. Raised to 718 by gate-stale-input-refusal, whose review found the worktree refusal failing OPEN on any git error; the eleven added tests break git itself rather than the pathspec. Raised to 724 by gate-suite-flakiness: three cover the named deadline that replaced a bare hook timeout, two hold eslint.config.mjs's global ignore list against the template's, and the tenth — added in review — holds each boundary block's own ignores against the template's. An ignore switches a rule off for a path, so the two ignore lists were the ways left to soften a boundary rule that the block comparison could not see; closing the global one left the per-block one open at the same price. Left at its creation value of 317 while the suite grew to 487, the ratchet would have admitted the deletion of every kernel test — review caught that. Bump it in the PR that adds the tests, not later. Raised to 741 by deadlines-fingerprint-and-severity-result: three cases holding the colon escape and the discriminated verdict. Raised to 800 by gate-size-impl-exclude-comments: fifty-nine cases holding the comment discount in both directions, three of which run gate.sh end to end over a 450-line diff. Twenty-four of them arrived with Ahmed's ruling of 2026-08-17 that the discount covers every source language and that tasks/backlog.yaml is out of the code budget: seven hold the JavaScript reading, fourteen hold the shell one — a # in a quoted string, in a heredoc, on the shebang, at no word boundary, and a file the reader refuses rather than counts — and three hold the backlog exclusion against size-impl while size-total still fails on it. Merged to 818 by timezone-reject-utc meeting main: 800 on main plus this branch's 18, measured rather than taken from either side of the conflict — both branches added tests, so the higher of the two was still wrong for the merged state. Merged to 893 by module-deadlines-sweep meeting main: 818 on main plus this branch's 75, measured rather than taken from either side — both branches added tests, so the higher of the two was still wrong for the merged state. Raised to 959 by kernel-repository-integration-tests: 66 cases over the six kernel repositories, every exported function of each, run against an engine the suite obtains rather than skips for (ADR-038). Raised to 989 by module-deadlines-repository: 18 integration cases over the four DeadlineStore members and the two unique indexes, plus the 12 source-reading cases that carried in with tests/modules/deadlines/repository.test.ts. Measured on the merged tree, not added to either side.",
    "",
    "Bump this number when a task legitimately adds tests. A task that",
    "legitimately REMOVES them — consolidating cases into one it.each, deleting",
    "a module and its suite — records test_count_waiver on its backlog node with",
    "a reason, exactly as size_total does.",
    "",
    "----",
    "",
    "coverage_bp — total repository line coverage, which must not decrease. Set to 9800 by kernel-repository-integration-tests, deliberately BELOW the 99.24% that run measured. A ratchet pinned to its own high-water mark is a hair-trigger: this repository has 396 executable lines, so one line is 25 basis points, and CI measured 98.98% where the same commit measured 99.24% locally. The floor exists to catch a REGRESSION, and a regression is many lines, not one. 9800 keeps roughly four lines of headroom and still stands 1341 points above the 8459 this task inherited.",
    "",
    "In basis points, hundredths of a percent: 7920 means 79.20%. An integer, so",
    "the gate compares integers and never flaps between 79.2 and 79.20, and so",
    "no rounding rule has to be agreed between whoever writes this file and",
    "whoever reads it. Computed from the DA: records in coverage/lcov.info as",
    "covered * 10000 / total, truncated.",
    "",
    "The second of two coverage checks, and the one that is not about your task.",
    "gate.sh measures the lines a task CHANGED against 90 (money, compliance,",
    "unknown risk) or 70 (low) — see ADR-030, the floor was never the defect. But",
    "diff coverage alone lets the total rot one uncovered file at a time, every",
    "task green on its own lines, so this ratchets the whole repository: it fails",
    "on a DECREASE only and never on standing still.",
    "",
    "Set to 7920 by gate-coverage-strict-path, then raised by module-deadlines-civil-date and module-deadlines-thresholds in turn, and to 8226 by timezone-reject-utc (283 of 344 lines) — the zone predicates and the advice sentence are pure, so the tests reach them without a database. 7920 was what the repository",
    "measured at the time (240 of 303 lines). The number is low because six",
    "lib/kernel/*/repository.ts files are Prisma plumbing no unit test can reach.",
    "They are deliberately NOT excluded from coverage — raising them is",
    "kernel-repository-integration-tests, and that node bumps this key. Raised to 9924 by that node (393 of 396 lines): all six are at 100% of lines, measured with `npx vitest run --coverage` and computed from the DA records in coverage/lcov.info.",
    "",
    "THIS INTEGER IS ITSELF PART OF THE RATCHET. The gate resolves it from the",
    "PR base as well as from the working tree, because a ratchet read only from",
    "the tree grades itself: edit the number down and the percentage no longer",
    "has to clear anything, the waiver below never runs, and the decrease is",
    "reported as a pass. A value lower here than on the base is treated exactly",
    "as an actual decrease — same waiver, same reason. Raising it is free.",
    "",
    "Bump it in the PR that raises coverage, not later; the gate prints the new",
    "value when it sees one. A task that legitimately LOWERS coverage records",
    "coverage_waiver on its backlog node with a coverage_waiver_reason, in basis",
    "points, exactly as test_count_waiver and size_total do."
  ]
}
