{
  "tests": 1350,
  "tests_scoped": 1101,
  "coverage_bp": 9900,
  "why": [
    "The number of tests the suite must not drop below.",
    "",
    "A suite can shrink without any gate noticing. A file whose import breaks",
    "still reports its neighbours as green; a describe block deleted in a merge",
    "resolution leaves no trace; a refactor can lose cases silently. The other",
    "gates all answer 'did what ran pass' — none answers 'did everything still",
    "run'. This is a ratchet, not a target: it fails on a DECREASE only.",
    "",
    "TWO FLOORS, BECAUSE THERE ARE TWO SUITES. Since gate-scopes-the-suite-to-the-diff, a",
    "diff touching none of scripts/, eslint.config.mjs, templates/, .github/workflows/,",
    "tests/gates/, tests/scaffold/, package.json, package-lock.json or tsconfig.json runs",
    "the suite without tests/gates and tests/scaffold — the pipeline testing itself, 9 of",
    "52 test files and 249 of 1350 tests. A path is a trigger if changing it could change",
    "what those tests should say, and that rule reaches further than the machinery: the",
    "pipeline's own tests are triggers for themselves, and package.json, tsconfig.json and",
    "the lockfile are triggers because tests/scaffold reads the real ones at run time.",
    "",
    "The scoped run collects 249 fewer tests, so \"tests\" is the wrong floor for it and",
    "\"tests_scoped\" is the floor it is graded against. gate.sh picks the key in the same",
    "branch that decides the exclusion, and then checks the floor in hand against the",
    "vitest report's own list of files, so the mode graded is the mode that ran rather",
    "than the mode that was asked for.",
    "",
    "REPRODUCE EACH NUMBER WITH THE RUN THAT MODE MAKES, never by subtracting from the",
    "other. tests_scoped is `npx vitest run --exclude 'tests/gates/**' --exclude",
    "'tests/scaffold/**'`, read from numTotalTests; tests is `npx vitest run`. Measured at",
    "320b366 on 2026-08-19: 1350 full over 52 files, 1101 scoped, and the 249 between them",
    "are the 9 files under tests/gates and tests/scaffold. A test added under tests/gates",
    "moves the full floor and leaves the scoped one where it is, and that asymmetry is the",
    "check that each number came from its own run.",
    "",
    "KEEP tests_scoped STRICTLY BELOW tests. The scoped run is a strict subset of the",
    "full one, so any other ordering means one key was bumped and the other forgotten —",
    "and a FULL run graded against a scoped floor would pass the loss of two hundred",
    "tests in silence. gate.sh refuses to grade at all when that ordering breaks. Bump",
    "BOTH in the PR that adds tests: the full one to what a pipeline-touching run",
    "measured, the scoped one to what a product run measured.",
    "",
    "Bumped to 487 with kernel-entities-law, to 574 with gate-coverage-strict-path, and again with module-deadlines-civil-date and module-deadlines-thresholds — the two halves of one split, which landed separately and each added 53. Raised to 718 by gate-stale-input-refusal, whose review found the worktree refusal failing OPEN on any git error; the eleven added tests break git itself rather than the pathspec. Raised to 724 by gate-suite-flakiness: three cover the named deadline that replaced a bare hook timeout, two hold eslint.config.mjs's global ignore list against the template's, and the tenth — added in review — holds each boundary block's own ignores against the template's. An ignore switches a rule off for a path, so the two ignore lists were the ways left to soften a boundary rule that the block comparison could not see; closing the global one left the per-block one open at the same price. Left at its creation value of 317 while the suite grew to 487, the ratchet would have admitted the deletion of every kernel test — review caught that. Bump it in the PR that adds the tests, not later. Raised to 741 by deadlines-fingerprint-and-severity-result: three cases holding the colon escape and the discriminated verdict. Raised to 800 by gate-size-impl-exclude-comments: fifty-nine cases holding the comment discount in both directions, three of which run gate.sh end to end over a 450-line diff. Twenty-four of them arrived with Ahmed's ruling of 2026-08-17 that the discount covers every source language and that tasks/backlog.yaml is out of the code budget: seven hold the JavaScript reading, fourteen hold the shell one — a # in a quoted string, in a heredoc, on the shebang, at no word boundary, and a file the reader refuses rather than counts — and three hold the backlog exclusion against size-impl while size-total still fails on it. Merged to 818 by timezone-reject-utc meeting main: 800 on main plus this branch's 18, measured rather than taken from either side of the conflict — both branches added tests, so the higher of the two was still wrong for the merged state. Merged to 893 by module-deadlines-sweep meeting main: 818 on main plus this branch's 75, measured rather than taken from either side — both branches added tests, so the higher of the two was still wrong for the merged state. Raised to 959 by kernel-repository-integration-tests: 66 cases over the six kernel repositories, every exported function of each, run against an engine the suite obtains rather than skips for (ADR-038). Raised to 989 by module-deadlines-repository: 18 integration cases over the four DeadlineStore members and the two unique indexes, plus the 12 source-reading cases that carried in with tests/modules/deadlines/repository.test.ts. Measured on the merged tree, not added to either side. Raised to 1080 by deadlines-alert-contract-scope-and-failure: 91 cases over ADR-040, written blind to the implementation from the ADR and `git show HEAD:` alone, and proved to discriminate by running them at the pre-change commit in a detached worktree — 62 of 91 red there, for the right reasons, with every one of the node's four assertions red at least once. Raised to 1093 by deadlines-alert-contract-vocabulary: 13 cases holding ADR-043 through three independent mechanisms — a source rule read by the TypeScript parser, a compile-level assignability check against an engine written in generic vocabulary alone, and a runtime sweep read through `area` only — plus a detector self-test so a green sweep cannot be a broken reader, and three cases asserting the OTHER side of the line, that OpsMind still names a jurisdiction in its own inputs, its calendar and its column. Six were red at the pre-rename commit. Raised to 1099 by gate-eslint-ignores-generated-output: six cases holding that generated coverage output is outside the lint, that the ignore saying so did not widen into anything a human wrote, and that the exception is still argued for where a reviewer reads it — an ignore switches every rule off for a path, so the risk this node carries is an unaudited ignore rather than an unignored directory. Raised to 1111 by gate-service-boundary-rules: twelve cases over the enforcement layer for lib/services/, six of them red at the pre-change commit. The load-bearing one proves the HALF-FIX is not enough: widening eslint alone permits a service repository to import the client while check-boundaries.sh reports nothing about it, and the suite stays red under every form of that mutation an adversarial reviewer tried. How many cases stay red depends on which half-fix is applied, so no count is stated here — the reproducible claim is that none of them goes green. Raised to 1150 by service-alerts-surface-and-lifecycle: 39 cases over the Alert Manager's public surface and its four-state lifecycle, written blind to an implementation that did not exist at HEAD. Because red-before-the-fix proves nothing for a wholly new file, discrimination was shown by mutating the finished code eight ways instead: splitting the fingerprint, letting severity fall, adding a fifth state, refusing an unknown policy, narrowing the open states, changing an argument's arity, reintroducing the jurisdiction vocabulary, and deleting the stale flag. Every one was killed, two of them by the type checker rather than at run time. Raised again to 1192 by the same node after the coverage gate refused it at 50 percent of its changed lines: nine exported transitions had no caller, so acknowledged-and-suppressed-are-open was proved by membership in a list rather than by calling acknowledge and seeing what came back. 42 further cases exercise the transitions as behaviour, and eight more mutations were killed - the one that returned resolved from acknowledge killed eight cases on its own. Raised to 1195 after adversarial review failed the node: the compile binding proving the client satisfies the port was ONE-DIRECTIONAL, because TypeScript lets an implementation with fewer parameters satisfy a wider signature. Dropping the scopes argument - the completeness declaration two nodes were spent building - compiled clean and passed all 81 cases. A reverse binding makes it mutual, which is the word the assertion uses, and it is invisible to the runner by construction: the type checker is what fails. Raised to 1247 by service-alerts-store: 36 cases against a real engine and 16 reading the repository's source. Fourteen mutations were applied to the store and schema at commit 6ca7928 and all fourteen died, none skipped for a compilation reason. Two were BEHAVIOUR-NEUTRAL by construction - an updateMany and a deleteMany matching zero rows - and every database case passed under both; each was killed by one static assertion derived from the card's sentence that an event is never updated and never deleted, which has no runtime consequence in this schema. A third deleted the row on resolve while returning an object that looked correctly resolved, and died five times over because the cases read the store back rather than trusting the return value. Raised to 1254 by gate-boundaries-blind-to-a-transaction: seven cases over the ownership check, four of them red at b40ff55 and three green there and after, because a fix that started reporting a legitimate repository would satisfy every discriminator and still be wrong. The guards were proved non-vacuous by applying two WRONG fixes to a throwaway candidate: a whitespace-only split of the declaration failed five cases, caught only by the green-at-HEAD guards, and a handle hardcoded to tx failed exactly one. Raised to 1350 by gate-scopes-the-suite-to-the-diff: 27 cases over the scoping rule itself, all of them under tests/gates/, which is why the full floor moves by 27 and tests_scoped does not move at all. That asymmetry is the arithmetic check that the two floors are read from the runs they claim to describe. Raised to 1306 by service-alerts-raise, the node that makes a merged module deliver: 52 cases against a real engine and a real PostgreSQL, ALL of them red at 7f19e6e. Not case by case - both files fail during collection, because a client that satisfies the port, returns promises and records nothing is rejected rather than measured, and that is exactly what HEAD had. Eight mutations, eight killed. One of them, deleting the resolved event so a closure leaves no record to survive a reopen, is killed by exactly one case. Raised again to 1323 after adversarial review failed the node: raiseKind decides what every event SAYS happened, and nothing asserted any of its three outcomes - two mutations of it survived all 188 cases while diff-cov read 100 percent, because those lines execute and executing is not being asserted about. Seventeen cases added, all seven kind mutations killed and none killed before. The finding generalised: createMemoryAlertStore was referenced by no test at all, though it is a public export and the default store, and three mutations of its dedupe logic now die.",
    "",
    "Bump this number when a task legitimately adds tests. A task that",
    "legitimately REMOVES them — consolidating cases into one it.each, deleting",
    "a module and its suite — records test_count_waiver on its backlog node with",
    "a reason, exactly as size_total does.",
    "",
    "----",
    "",
    "coverage_bp — total repository line coverage, which must not decrease. Set to 9800 by kernel-repository-integration-tests, deliberately BELOW the 99.24% that run measured. A ratchet pinned to its own high-water mark is a hair-trigger: this repository has 396 executable lines, so one line is 25 basis points, and CI measured 98.98% where the same commit measured 99.24% locally. The floor exists to catch a REGRESSION, and a regression is many lines, not one. 9800 keeps roughly four lines of headroom and still stands 1341 points above the 8459 this task inherited.",
    "",
    "In basis points, hundredths of a percent: 7920 means 79.20%. An integer, so",
    "the gate compares integers and never flaps between 79.2 and 79.20, and so",
    "no rounding rule has to be agreed between whoever writes this file and",
    "whoever reads it. Computed from the DA: records in coverage/lcov.info as",
    "covered * 10000 / total, truncated.",
    "",
    "The second of two coverage checks, and the one that is not about your task.",
    "gate.sh measures the lines a task CHANGED against 90 (money, compliance,",
    "unknown risk) or 70 (low) — see ADR-030, the floor was never the defect. But",
    "diff coverage alone lets the total rot one uncovered file at a time, every",
    "task green on its own lines, so this ratchets the whole repository: it fails",
    "on a DECREASE only and never on standing still.",
    "",
    "Set to 7920 by gate-coverage-strict-path, then raised by module-deadlines-civil-date and module-deadlines-thresholds in turn, and to 8226 by timezone-reject-utc (283 of 344 lines) — the zone predicates and the advice sentence are pure, so the tests reach them without a database. 7920 was what the repository",
    "measured at the time (240 of 303 lines). The number is low because six",
    "lib/kernel/*/repository.ts files are Prisma plumbing no unit test can reach.",
    "They are deliberately NOT excluded from coverage — raising them is",
    "kernel-repository-integration-tests, and that node bumps this key. Raised to 9924 by that node (393 of 396 lines): all six are at 100% of lines, measured with `npx vitest run --coverage` and computed from the DA records in coverage/lcov.info. Raised from 9800 to 9900 by deadlines-alert-contract-scope-and-failure, which measured 9928 (414 of 417). NOT to the measured peak, and the arithmetic is the reason: 417 executable lines make one line 24 basis points, so a ratchet pinned at 9928 fails on a single line and CI has previously measured about one line lower than the same commit measured locally. 9900 clears that delta and still closes most of the 128-point gap the old 9800 was leaving open. The hair-trigger argument that kept it at 9800 was about gate-total-cov-nondeterministic, which is now closed as resolved, so the loose floor no longer has that justification — only the one-line CI delta, which is what 9900 is sized against.",
    "",
    "THIS INTEGER IS ITSELF PART OF THE RATCHET. The gate resolves it from the",
    "PR base as well as from the working tree, because a ratchet read only from",
    "the tree grades itself: edit the number down and the percentage no longer",
    "has to clear anything, the waiver below never runs, and the decrease is",
    "reported as a pass. A value lower here than on the base is treated exactly",
    "as an actual decrease — same waiver, same reason. Raising it is free.",
    "",
    "Bump it in the PR that raises coverage, not later; the gate prints the new",
    "value when it sees one. A task that legitimately LOWERS coverage records",
    "coverage_waiver on its backlog node with a coverage_waiver_reason, in basis",
    "points, exactly as test_count_waiver and size_total do."
  ]
}
